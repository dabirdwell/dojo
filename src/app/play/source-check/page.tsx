import SourceCheckGame from "@/components/source-check/SourceCheckGame";

export const metadata = {
  title: "Source Check — Dojo",
  description: "Evaluate news headlines and claims for credibility, bias, and verification strategies.",
};

export default function SourceCheckPage() {
  return <SourceCheckGame />;
}
