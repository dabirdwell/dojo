import ArgumentMapGame from "@/components/argument-map/ArgumentMapGame";

export const metadata = {
  title: "Argument Mapping — Dojo",
  description:
    "Visually map argument structure. Identify claims, premises, evidence, and fallacies. The highest-impact critical thinking technique.",
};

export default function ArgumentMapPage() {
  return <ArgumentMapGame />;
}
