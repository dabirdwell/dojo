import RealWorldGame from "@/components/real-world/RealWorldGame";

export const metadata = {
  title: "Real World Practice — Dojo",
  description:
    "Spot fallacies, evaluate sources, and steelman arguments using real-world scenarios.",
};

export default function RealWorldPage() {
  return <RealWorldGame />;
}
