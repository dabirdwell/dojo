import WhiteBeltLearn from "@/components/learn/WhiteBeltLearn";

export const metadata = {
  title: "White Belt — Dojo",
  description:
    "Six fallacies you'll meet first. Each lesson teaches the trick, shows you where it lives in the wild, then tests you on it.",
};

export default function WhiteBeltPage() {
  return <WhiteBeltLearn />;
}
